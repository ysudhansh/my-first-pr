#include <bits/stdc++.h>

int main()
{
    int n;
    scanf("%d",&n);
    long long  arr[n+1],sum=0;
    for(int i=0;i<n;i++)
    {
        scanf("%lld",&arr[i]);
    }
    for(int i=0;i<n;i++)
        sum=sum+arr[i];
    
    printf("%lld",sum);
}

